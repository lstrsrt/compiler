#{returns(41)}
fn collatz(x: s32) -> s32 {
    steps := 0
    while x != 1 {
        if !(x % 2) {
            x = x / 2
        }
        else {
            x = 3 * x + 1
        }
        steps += 1
    }
    return steps
}
return collatz(139)
